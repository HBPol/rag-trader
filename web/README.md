# RAGTrader Web

Next.js + TypeScript frontend for RAGTrader. The current scaffold provides
our layout shell, styling entry point, and a simple component test so we
can iterate with TDD in upcoming issues.

## Development

```bash
pnpm install # or npm install / yarn install
pnpm test
```
